{
  "name": "chatterbox-server",
  "version": "1.0.0",
  "description": "The server for a chat app where private conversations are prioritised.",
  "main": "dist/index.js",
  "scripts": {
    "start": "node .",
    "build": "rimraf dist && tsc",
    "dev": "rimraf dist && tsc-watch --onSuccess \"npm run start\""
  },
  "author": "Beef",
  "license": "BSD-3-Clause",
  "dependencies": {
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.4"
  }
}
